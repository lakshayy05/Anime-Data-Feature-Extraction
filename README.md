# Anime-Data-Feature-Extraction
A data processing project focused on Feature Engineering and extraction using Python and Pandas  The project takes a raw dataset (`anime.csv`) where metadata like episode counts and broadcast dates are embedded inside the 'Title' string, and extracts them into clean, numerical columns.
